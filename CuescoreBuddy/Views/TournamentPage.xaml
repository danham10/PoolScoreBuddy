<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    HideSoftInputOnTapped="True"
    x:Class="CuescoreBuddy.Views.TournamentPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:CuescoreBuddy.ViewModels"
    xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Title="TournamentPage"
    Shell.NavBarIsVisible="False">
    <ContentPage.BindingContext>
        <vm:TournamentViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Behaviors>
        <mct:EventToCommandBehavior Command="{Binding AppearingCommand}" EventName="Appearing" />
    </ContentPage.Behaviors>
    <VerticalStackLayout Margin="10,0,10,0">
        <Label
            Margin="0"
            FontSize="Medium"
            HorizontalOptions="FillAndExpand"
            Text="Enter a tournament number like this one" />
        <Image Source="tournament_link.png" />
        <Entry
            Margin="0"
            FontSize="Medium"
            Placeholder="Enter tournament number here"
            MaxLength="16"
            Keyboard="Numeric"
            Text="{Binding TournamentId}" />
        <Button
            Command="{Binding TournamentLoadCommand}"
            HorizontalOptions="FillAndExpand"
            Text="Load tournament" />
        <ActivityIndicator IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}" />
    </VerticalStackLayout>
</ContentPage>